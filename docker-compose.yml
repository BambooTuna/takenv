version: '3.8'

services:
  takenv:
    build:
      context: .
      dockerfile: devcontainer/Dockerfile
    container_name: takenv-dev
    volumes:
      - .:/workspace
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
    stdin_open: true
    tty: true
