## 言語設定

思考過程や結果は特に指定がなければ必ず日本語で行うこと。

## 実装レベルの語彙

「モック」「ざっくり」「とりあえず」と言われたら最小限実装に留める。

## コミットメッセージフォーマット

`<type>: <description>`（type: feat / fix / refactor / docs / test / chore / perf / ci）。
アトリビューションは ~/.claude/settings.json でグローバルに無効化済み。
